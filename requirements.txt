# Разведочный скрипт для ЭТП Газпромбанка
# Python 3.10+ (проверено на 3.13)

# Автоматизация Chrome
selenium>=4.25,<5
webdriver-manager>=4.0,<5

# Работа с Windows (COM-интерфейс CAdES, реестр, процессы)
pywin32>=308 ; sys_platform == "win32"

# Проверка запущенных процессов Chrome
psutil>=6.0

# HTTP-клиент (для проб API-endpoint'ов)
requests>=2.31

# Excel-вывод для финального парсера
openpyxl>=3.1

# Word-вывод таблиц анализа карточек
python-docx>=1.1

# Извлечение текста из PDF и архивов документации для анализа
pypdf>=6.0
py7zr>=1.0
rarfile>=4.2
python-pptx>=1.0
odfpy>=1.4
pymorphy3>=2.0

# Автозаполнение нативного окна Госплагина/КриптоПро (пароль к контейнеру)
# Работает только на Windows. На Linux/Mac можно не устанавливать.
pywinauto>=0.6.8; sys_platform == "win32"

# --- RAG-анализ карточки ЭТП ГПБ (чанки → multilingual-e5-small → FAISS → извлечение полей по одному).
# Если не установить — анализ остаётся в классическом режиме (один большой запрос к LLM).
sentence-transformers>=3.0
faiss-cpu>=1.8
pymupdf>=1.24
numpy>=1.26

# Сборка exe (Windows): pip install pyinstaller && python -m PyInstaller --clean --noconfirm ETP_GPB_Search.spec
# Готовая программа — каталог dist/ETP_GPB_Search/ целиком (ETP_GPB_Search.exe + _internal и др.).
#
# Запуск с сетевого диска \\server\share часто даёт «Failed to load python313.dll / LoadLibrary»:
#   — скопируйте всю папку на локальный диск (C:\...) или используйте один файл:
#     python -m PyInstaller --clean --noconfirm ETP_GPB_Search_onefile.spec  → dist\ETP_GPB_Search.exe
#   — на целевом ПК установите «Microsoft Visual C++ Redistributable 2015–2022 (x64)».
